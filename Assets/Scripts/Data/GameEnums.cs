public enum CameraRayOutObject
{
    Nothing,
    Ground,
    Interactable,
    Enemy
}

public enum GameState
{
    Moving,
    OnDialogue,
    OnPause,
    OnInventory
}

public enum SkillType
{
    Active,
    Passive
}

public enum SkillNodeState
{
    Locked,
    Unlocked,
    Acquired
}